# PRD.md — WorkShop 会议工作台

> 文档类型：产品需求文档（PRD）v0.1
> 版本日期：2026-09-17（Day 4 产出）
> 依据：`research.md`（Day 3 需求研究）、Day 2 产品方向、本次用户补充 brief
> 状态：MVP 范围确认稿（仅需求描述，不含代码）

> 附注：2026-09-20 补充「阶段二愿景功能：多人协作与颜色区分」（见第 6.1 节），MVP 范围不变。
> 附注：2026-09-20 Day 7 设计决策（F1 落地形态，见 §4 F1 与 §6）——采用「双表（工作事项表 / 日常事项表）+ 三模式（工作 / 日常 / 总表）」；**颜色 = 日程类型**，**记号 = 部门 / 人物（仅总表显示）**；工作表内重叠 = 冲突、日常表内重叠 = 提醒（允许）、**跨表（工作↔日常）重叠 = 冲突也要警告**。

---

## 1. 产品概述

- **一句话定位**：面向政务人员与基层工作人员的轻量会议工作台，用一条统一的周时间线把零散的会议与行程编排到一起，解决「排会议难、行程不便于统一」的问题。
- **目标用户**
  - **政务人员**：在多个会议、调研、接待之间协调，常跨部门、口径不一。
  - **工作人员（单位 / 团队事务岗）**：负责统筹安排会议与行程，手工表格易错、难以统一。
- **核心痛点**
  1. **排会议困难**：时间易冲突，房间 / 人员难协调。
  2. **行程不便于统一**：多方口径不一致，缺少单一时间轴视图。
- **差异化（相对飞书 / 钉钉 / 腾讯会议）**：以「周时间线」为统一内核，轻量、聚焦政务与工作场景的会议 / 行程编排，不追求全家桶。

---

## 2. 本期范围（MVP）

MVP 聚焦以下三件事，均为**单端 Web（桌面优先）**：

1. **时间线安排事项**（最小时间线粒度 = 一周）
2. **导入图片 + 表格识别**（调用外部免费 AI 接口）
3. **会议安排要求窗口**（结构化的会议需求录入）

**数据落地**：事项字段可存入**腾讯文档**。

**不在本期**：完整移动端适配、PreText 个性化渲染、腾讯会议真实对接、AI 会议纪要、账号 / 云同步等（见第 6 节）。

---

## 3. 核心功能清单

| 编号 | 功能 | 优先级 | 说明 |
|---|---|---|---|
| F1 | 时间线安排事项 | P0 | 周粒度时间线，增 / 删 / 改会议与行程 |
| F2 | 导入图片 + 表格识别 | P0 | 上传图片 / 表格，外部免费 AI 识别为结构化事项 |
| F3 | 会议安排要求窗口 | P0 | 录入会议需求（时间、人员、场地等）的表单窗口 |
| F4 | 数据存入腾讯文档 | P0 | 事项字段导出 / 同步至腾讯文档 |
| L0 | 上下栏 + 左右分栏布局 | P0 | 上 / 下栏 + 左时间线 / 右详情 |

---

## 4. 功能详情（用户动作 → 可观察结果）

### F1 时间线安排事项
- **用户动作**：在时间线上点击某天 / 某时段，选择「新建事项」，选择所属表（工作事项表 / 日常事项表，**无总表选项**——总表是二者之和、仅供查看）、填写标题、类型、起止时间、部门 / 人物归属。
- **可观察结果**：时间线对应位置出现该事项色块（**颜色 = 日程类型**：会议蓝 / 行程橙 / 课表绿 / 运动青 / 生活紫 / 待安排灰）；右侧栏显示其详情；底部表签可切换「工作事项表 / 日常事项表 / 总表」三种模式，周视图可左右切换上一周 / 下一周；同一天内卡片按开始时间升序排列。
- **双表与三模式（Day 7 设计）**
  - **工作事项表**：部门活动 / 会议；按部门 / 成员用**记号**（★我/部门、△综合部、○业务部、□技术部、☆外联部）区分归属，记号**仅总表显示**。
  - **日常事项表**：课表 / 非部门活动（私人项统一归「我(私人)」），类型用颜色区分，不显示记号。
  - **总表**：两张表合并视图，因混合来源故**显示部门 / 人物记号**以区分归属。
- **重叠规则**
  - 工作表内时间重叠 = **冲突**（红框 ⚠，不该重叠）。
  - 日常表内时间重叠 = **提醒**（橙虚框 ⚠，允许重叠，后续接 AI 查重叠影响）。
  - **跨表（工作 ↔ 日常）时间重叠 = 冲突也要警告**（紫框 ⚠）。
  - 状态栏汇总展示「工作⚠ / 日常⚠ / 跨表⚠」计数。
- **边界**：最小时间线单位为「一周」；首版可内存态，但最终字段需可导出腾讯文档。

### F2 导入图片 + 表格识别
- **用户动作**：点击「导入」，选择本地图片或表格文件，确认「识别」。
- **可观察结果**：系统调用外部免费 AI 接口，返回识别出的事项列表（标题、时间、参与人等）预览，用户勾选后「加入时间线」。
- **边界**：识别结果需人工确认后方可入线；外部接口失败时有错误提示与重试入口。

### F3 会议安排要求窗口
- **用户动作**：点击「新建会议要求」，在弹出窗口填写：期望日期范围、时长、参会人数 / 名单、场地要求、设备要求、备注。
- **可观察结果**：窗口内实时显示已填字段与必填校验；提交后生成一条「待安排会议」事项进入时间线待排区。
- **边界**：必填项缺失时禁止提交并高亮提示。

### L0 上下栏 + 左右分栏布局
- **上栏**：应用标题 / 全局操作（新建、导入、周切换）。
- **下栏（状态栏）**：当前周、事项计数、同步状态。
- **左栏**：周时间线图（按天分列 / 分行）。
- **右栏**：选中事项的详细介绍（标题、时间、参与人、要求、来源）。
- **移动端（设计约束，非实现）**：窄屏下应堆叠为单列（时间线在上、详情在下）；**完整移动端适配实现列为后续**（见第 6 节）。

---

## 5. 数据模型与字段（含存入腾讯文档）

事项（Item）字段：

- `id`：唯一标识
- `title`：标题
- `type`：会议 / 行程 / 待安排
- `start` / `end`：起止时间
- `week`：所属周
- `attendees`：参与人
- `venue`：场地
- `equipment`：设备
- `note`：备注
- `source`：来源（手动 / 导入识别）
- `status`：已排 / 待排

**同步**：上述字段可导出 / 同步至**腾讯文档**（一张表，每行一个事项）。

> **阶段二扩展字段（详见第 6.1 节愿景功能）**：为支持多人协作与颜色区分，后续拟在事项上增加 `owner`（创建者）、`org`（所属组织 / 标签）、`color`（着色）三个字段，使同一条时间线可区分不同个人与组织。本期 MVP 不实现。

> **Day 7 设计补充（MVP 已实现字段）**：事项增加 `table`（`work` 工作事项表 / `daily` 日常事项表，决定所属表与三模式归属）与 `ownerKey`（部门 / 人物记号归属，如 `self`/`depta`/…/`me`）；`type` 细化为会议 / 行程 / 课表 / 运动 / 生活 / 待安排（见 §4 F1）。记号渲染走统一出口，后续可替换为部门徽章 / 印章图片。

---

## 6. ★ 砍掉 / 暂缓的功能及原因

| 功能 | 状态 | 原因 |
|---|---|---|
| 腾讯会议真实对接 / **直接接口**（预约 / 入会 / 纪要） | 暂缓→阶段二 | MVP 先验证时间线与编排价值；架构预留**直接对接接口**（不只 OCR + 腾讯文档）；真实会议链路依赖生态对接，放到价值验证后 |
| AI 会议纪要与会议记录 | 暂缓 | 依赖元宝等 AI 能力，超出本期范围 |
| 账号体系 / 云同步 / 多端 | 暂缓 | 首版单端 Web，先用腾讯文档做轻量数据落地，避免提前投入账号架构 |
| 自研视频会议引擎 | 砍掉 | 不自造，后续对接现成会议工具 |
| 企业级权限 / 会议室预订系统 | 砍掉 | 不追求飞书级全家桶 |
| PreText 界面渲染 | 暂缓→后续 | 用户明确列为后续（deferred 已确认）；MVP 用默认渲染即可验证编排价值；代码已预留 `renderItemText()` 接缝 |
| 手机端完整适配 | 暂缓→后续 | 设计约束记入 PRD，但完整响应式实现放到后续迭代 |
| 会议提醒（原 research MVP 项） | 暂缓→后续 | 本期三大功能未含提醒；待核心编排稳定后补充 |
| 多人协作 + 颜色区分（个人 / 组织标签） | 阶段二愿景 | 直接服务核心痛点「协调难」；机制上靠腾讯文档共享层实现，不强制自建账号体系（见 6.1 专节） |
| **日常表重叠接 AI 查重叠影响** | 后续 | 日常表允许重叠（橙虚框提醒），后续接入 AI 评估重叠的实际影响 |
| **记号替换为部门徽章 / 印章** | 后续 | 记号渲染走统一出口（`markOf()`），后续可替换为部门徽章 / 印章图片 |
| **拖动交互：拖到删除区删除 / 拖到另一天转移日期** | 后续 | 交互增强，卡片支持拖拽删除与跨天拖拽改期，非 MVP |
| **可自定义部门设置 + 不同账号** | 后续 | 部门（名称 / 记号 / 归属）可由用户自定义；并支持不同账号（多人），与阶段二协作呼应 |
| **PreText 上下周切换滚动过渡效果（暂定）** | 后续 | 后续用 PreText 给「上 / 下周切换」做滚动过渡动画，提升流畅度与动态效果（与 PreText 界面合并考虑） |

---

### 6.1 ★ 阶段二愿景功能：多人协作与颜色区分（重点）

**目标**：让多名政务 / 工作人员在同一周时间线上协作排会，用颜色区分个人与组织（标签），一眼看清「谁在哪、谁和谁撞了」，直接缓解核心痛点「排会议难、行程不便于统一」。

**机制（写清楚）**
- 每条事项在阶段二新增归属字段：`owner`（创建者）、`org`（所属组织 / 标签）、`color`（着色）。
- 底层共享**同一张腾讯文档**（沿用 §2 数据落地方案），利用腾讯文档原生多人协作能力实现多人同看同编；**不在 MVP 自建账号 / 实时同步后端**。
- 时间线渲染时按 `color` 着色：个人色块 + 组织分组两层语义；提供图例（legend）说明颜色含义。

**★ 重点（务必记住）**
1. **这是「阶段二」，不是 MVP。** MVP 仍是单人编排，先验证周时间线编排价值；协作层在其之后。
2. **协作通过「共享腾讯文档」实现，避免复活整套账号 / 云同步架构**——这是该想法能低成本落地的关键，也是它与 §6 原「账号体系 / 云同步」暂缓项不矛盾的原因。
3. **颜色语义 = 个人层 + 组织层**：个人色块区分「谁」，组织分组区分「哪个单位 / 团队」，两层可叠加。
4. **冲突检测是协调价值的核心交付**：多人同槽 / 同人时间重叠时，时间线视觉高亮冲突，便于提前协调。

**待定问题**
- 实时同步粒度：腾讯文档轮询刷新 vs webhook 推送。
- 权限模型：是否允许改他人条目、组织管理员角色。
- 颜色分配：自动分配 vs 用户自选，组织色优先级。

---

## 7. 验收标准（逐条可检查）

- **AC-1**：PRD 顶部明确标注项目为 WorkShop 会议工作台，含版本与日期。
- **AC-2**：目标用户明确为政务人员与工作人员，并写明其痛点（排会议困难、行程不统一）。
- **AC-3**：MVP 含三大功能：① 时间线安排（最小粒度 = 一周）② 图片 / 表格识别（外部免费 AI 接口）③ 会议安排要求窗口。
- **AC-4**：每个功能均写明「用户动作」与「可观察结果」。
- **AC-5**：页面布局描述为上下栏 + 左右分栏，左时间线、右详情。
- **AC-6**：数据字段含「存入腾讯文档」。
- **AC-7**：PreText 个性化渲染、手机端完整适配列为后续，不进入 MVP。
- **AC-8**：含独立「验收标准」章节，且每条可逐条检查。
- **AC-9**：含「砍掉 / 暂缓功能及原因」章节，每条有原因。
- **AC-10**：全文为需求描述，不含代码。

---

## 8. 非功能需求（简述）

- **兼容性**：现代桌面浏览器（Chrome / Edge）。
- **性能**：周时间线渲染、导入识别响应在可接受范围。
- **可用性**：导入识别结果必须人工确认后入线。

---

## 9. 开发顺序与里程碑（沿用 research.md）

前端 + 时间线实现 → 导入识别接入 → 会议要求窗口 → 数据落地腾讯文档 →（后续）移动端 / PreText / 腾讯会议对接 →（阶段二）多人协作与颜色区分（共享腾讯文档层）。

---

## 10. 待定问题

- 外部免费 AI 接口具体选型（Day 5 技术选型确定）。
- 腾讯文档同步是「导出」还是「实时同步」。
- 周时间线为横向还是纵向。

---

## 余力加练：用户故事（F1 示例）

> 作为政务工作人员，我希望在一条统一的周时间线上看到本周所有会议与行程，以便我一眼掌握安排、避免时间冲突。

**验收**：用户打开应用默认进入本周时间线；能看到已录入事项色块；点击色块右侧显示详情；可切换上 / 下周。
